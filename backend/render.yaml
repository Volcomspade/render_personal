services:
  - type: web
    name: checklist-api
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    plan: free
    region: oregon
    branch: main
    autoDeploy: true